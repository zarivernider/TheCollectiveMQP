#include <Arduino.h>
#include "servo.h"
#include "system_pi_pico.h"

void setup() {
  // put your setup code here, to run once:
  
  

}
// unsigned int minCount = 4762;
// unsigned int maxCount = 22857;
// void setServo(int degrees) {
//   unsigned int linCount = map(degrees, 0, 180, minCount, maxCount);
//   // writeReg(PWM_BASE | 0x0c, 16, 0, linCount);
// }
void loop() {
  // put your main code here, to run repeatedly:
  // setServo(45);
  // delay(5000);
  // setServo(0);
  // delay(5000);
}

  // writeReg(PWM_BASE | 0x0c, 0, 0, 0); // CH0_CC
